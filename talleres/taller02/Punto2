def Suma_grupo(start, nums, target):
    if target == 0:
        return True
    if start == nums.length:
        return False
    return Suma_grupo(start+1, nums, target-nums[start]) || Suma_grupo(start+1, nums, target)        
